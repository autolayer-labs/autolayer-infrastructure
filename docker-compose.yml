services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: autolayer
      POSTGRES_PASSWORD: autolayer
      POSTGRES_DB: autolayer
    ports:
      - "5434:5435"
    volumes:
      - autolayer_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autolayer -d autolayer"]
      interval: 5s
      timeout: 5s
      retries: 20

  autolayer:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./apps/api/.env
    environment:
      DATABASE_URL: postgresql://autolayer:autolayer@postgres:5435/autolayer
    ports:
      - "5001:5001"
    depends_on:
      postgres:
        condition: service_healthy
    command: sh -c "pnpm migrate && pnpm start"

volumes:
  autolayer_pg:
